name: DreamNet CI/CD (DISABLED - Using Google Cloud)

# LEGACY: This workflow previously deployed to Vercel
# PRIMARY DEPLOYMENT: Google Cloud Run (use pnpm deploy:gcp)
# This workflow is disabled to prevent automatic Vercel deployments

on:
  # DISABLED: No longer auto-deploying on push
  # push:
  #   branches:
  #     - main
  workflow_dispatch:  # Only runs if manually triggered

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    if: false  # Explicitly disabled
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install pnpm
        run: npm install -g pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Build
        run: pnpm build

      # DISABLED: Vercel deployment removed - use Google Cloud instead
      # - name: Deploy to Vercel
      #   uses: amondnet/vercel-action@v25
      #   with:
      #     vercel-token: ${{ secrets.VERCEL_TOKEN }}
      #     vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
      #     vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
      #     working-directory: .

