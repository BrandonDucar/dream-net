# Skip optional dependencies to avoid native module compilation issues
optional=false
# Ignore scripts during install
ignore-scripts=true
# Use npm registry
registry=https://registry.npmjs.org/
# Strict peer dependencies
strict-peer-dependencies=false
# Network timeout
fetch-timeout=60000
fetch-retries=5
