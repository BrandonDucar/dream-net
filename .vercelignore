# CRITICAL: Do NOT exclude client/ - it's the frontend!
# CRITICAL: Do NOT exclude pnpm-workspace.yaml - needed for pnpm filter

# Exclude old site specifically
apps/site-old/**

# Exclude other apps (but keep structure for workspace)
apps/api-forge/**
apps/dreamos/**
apps/hub/**
apps/seo/**
apps/sitebuilder/**

# Exclude server (backend only)
server/**

# Exclude contracts (smart contracts, not needed for frontend)
contracts/**

# Exclude scripts (dev tooling)
scripts/**

# Exclude docs (documentation)
docs/**

# Exclude markdown files (except README)
*.md
!README.md

# Exclude all node_modules (will be installed fresh)
node_modules
**/node_modules

# Exclude build outputs
dist
**/dist
build
**/build

# Exclude git and config files
.git
.gitignore
.replit
.env
.env.*

# Exclude test and dev files
**/*.test.ts
**/*.test.tsx
**/*.spec.ts
**/*.spec.tsx
coverage
**/coverage

# Exclude TypeScript build info
**/*.tsbuildinfo

# CRITICAL: These MUST be included for pnpm workspace to work:
# - client/ (the frontend)
# - packages/ (needed for Vite aliases)
# - shared/ (needed by client)
# - pnpm-workspace.yaml (needed for pnpm filter)
# - package.json (root, needed for workspace)
# - pnpm-lock.yaml (needed for install)
