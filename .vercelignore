# Minimal exclusions - only exclude what we absolutely don't need
# CRITICAL: client/, packages/, shared/, pnpm-workspace.yaml MUST be included

# Only exclude the disabled old site
apps/_site-old-DISABLED/**

# Exclude server
server/**

# Exclude contracts
contracts/**

# Exclude node_modules (will be installed)
node_modules
**/node_modules

# Exclude build outputs
dist
**/dist
build
**/build

# Exclude git
.git

# Exclude env files
.env
.env.*

# Everything else INCLUDING client/, packages/, shared/, pnpm-workspace.yaml, package.json, pnpm-lock.yaml MUST be included
