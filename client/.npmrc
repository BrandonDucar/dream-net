# Skip optional dependencies that cause native module compilation issues
optional=false
# Don't run install scripts (prevents native module compilation)
ignore-scripts=true
# Use pnpm
engine-strict=true
