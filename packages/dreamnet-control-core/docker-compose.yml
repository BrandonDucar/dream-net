version: '3.8'

services:
  # 🧠 THE CORTEX (DreamNet Core)
  dreamnet-core:
    build:
      context: ../../
      dockerfile: packages/dreamnet-control-core/Dockerfile
    image: dreamnet-core:sovereign-v1
    container_name: dreamnet_prime
    restart: always  # 🛡️ THE NIGHT WATCHMAN POLICY
    ports:
      - "3000:3000"
    env_file:
      - ../../.env
    environment:
      - NODE_ENV=production
    volumes:
      # Persistent Brain Storage (If using local JSON files)
      - ./brain_data:/app/brain_data
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G

  # 🩸 THE EVENT BUS (Redis - Optional/Future Proofing)
  # redis:
  #   image: redis:alpine
  #   restart: always
