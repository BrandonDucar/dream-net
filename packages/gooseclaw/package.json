{
  "name": "@dreamnet/gooseclaw",
  "version": "1.0.0",
  "description": "Manager orchestrator for 17,000 agent swarm via Cloudflare Workers",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env production",
    "deploy:staging": "wrangler deploy --env staging",
    "build": "echo 'No build required for Workers'",
    "test": "echo 'Testing Gooseclaw endpoint'"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^4.87.0"
  }
}
