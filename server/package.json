{
  "name": "@dreamnet/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "clean": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true })\""
  },
  "dependencies": {
    "blake3": "^2.1.7",
    "nanoid": "^5.0.7",
    "node-cron": "^3.0.3",
    "snarkjs": "^0.7.5"
  },
  "devDependencies": {
    "tsx": "^4.20.6"
  }
}

