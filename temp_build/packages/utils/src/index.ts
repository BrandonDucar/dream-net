/**
 * Placeholder utility exports for shared DreamNet helper functions.
 * Extend with real implementations as shared packages mature.
 */
export const noop = (): void => {
  // no-op utility to keep the package buildable until real helpers are added
};
